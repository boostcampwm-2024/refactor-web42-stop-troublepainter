{
  "name": "troublepainter-monorepo",
  "private": true,
  "scripts": {
    "typedoc": "typedoc",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server start:dev",
    "dev": "pnpm --filter client dev & pnpm --filter server start:dev",
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @troublepainter/core build",
    "build:full": "pnpm build:core && pnpm build",
    "docker": "docker-compose -f local-run/docker-compose.yml --env-file local-run/.env up",
    "docker:full": "pnpm docker:build && pnpm docker",
    "docker:build": "pnpm docker:client; pnpm docker:server",
    "docker:client": "source local-run/.env && set +a && docker build -f local-run/Dockerfile.nginx -t troublepainter-nginx .",
    "docker:server": "docker build -f local-run/Dockerfile.server -t troublepainter-server .",
    "start": "pnpm -r --parallel start",
    "start:client": "pnpm --filter client start",
    "start:server": "pnpm --filter server start",
    "run:full": "pnpm build:full && pnpm start",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typedoc": "^0.26.11",
    "typedoc-plugin-extras": "^3.1.0"
  },
  "dependencies": {
    "@troublepainter/core": "workspace:*"
  },
  "pnpm": {
    "overrides": {
      "got@<11.8.5": ">=11.8.5",
      "axios@>=0.8.1 <0.28.0": ">=0.28.0",
      "tough-cookie@<4.1.3": ">=4.1.3",
      "cross-spawn@<6.0.6": ">=6.0.6",
      "path-to-regexp@<0.1.12": ">=0.1.12",
      "nanoid@<3.3.8": ">=3.3.8"
    }
  }
}
